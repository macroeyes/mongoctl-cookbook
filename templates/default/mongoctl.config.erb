{
    "mongoDBInstallationsDirectory": "<%= @install_dir %>",

    "fileRepository": {
        "servers": "<%= @servers_config_filename %>",
        "clusters": "<%= @clusters_config_filename %>"
    }
}
